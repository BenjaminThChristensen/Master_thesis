#cloud-config
users:
  - name: dronepi
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    plain_text_passwd: 'pi'
    ssh_pwauth: true

hostname: dronepi
preserve_hostname: false
